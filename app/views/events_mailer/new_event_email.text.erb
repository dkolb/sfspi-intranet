New Point Nun Report Form Filed
===============================
<% ['date', 'name', 'venue', 'purpose', 'successes', 'challenges'].each do |f| -%>
  <%= @event.class.human_attribute_name(f) %>: <%= @event.send(f) %>
<%- end -%>
  Attendees:
<% @event.attendees.map(&:pseudonym).each do |a| -%>
    * <%= a %>
<%- end %>
  Point Members:
<% @event.point_members.map(&:pseudonym).each do |p| -%>
    * <%= p %>
<% end %>
  Reporting Member: <%= if @event.reporting_member.nil?
      "No Reporting Member"
    else
      @event.reporting_member.pseudonym
    end %>

You can view this event at the SFSPI Management site at:

<%= url_for(controller: 'events', action: 'show', id: @event_id) %>
